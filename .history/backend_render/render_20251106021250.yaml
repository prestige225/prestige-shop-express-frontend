services:
  - type: web
    name: prestige-shop-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python server_fixed.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.15