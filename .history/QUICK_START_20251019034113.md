# 🚀 Guide de Démarrage Rapide - Gestion des Commandes

## ⚡ En 3 étapes simples

### Étape 1: Créer la table dans MySQL

Exécutez ce script SQL dans votre base de données:

```sql
CREATE TABLE IF NOT EXISTS commandes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    numero_commande VARCHAR(50) NOT NULL UNIQUE,
    date_commande DATETIME DEFAULT CURRENT_TIMESTAMP,
    statut ENUM('en_attente', 'en_cours', 'livree', 'annulee') DEFAULT 'en_attente',
    montant_total DECIMAL(10,2) NOT NULL,
    adresse_livraison TEXT NOT NULL,
    telephone VARCHAR(20) NOT NULL,
    notes TEXT,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);
```

**OU** utilisez le fichier SQL fourni:
```bash
mysql -u usblj9n0kraq8uoc -p -h bracv1wswmu4vsqxycku-mysql.services.clever-cloud.com bracv1wswmu4vsqxycku < table_commandes.sql
```

### Étape 2: Démarrer le serveur Flask

```bash
cd "c:\Users\RCK COMPUTERS\Desktop\new work\prestige shop express"
python server_fixed.py
```

Vous devriez voir:
```
🚀 Démarrage du serveur Flask corrigé...
🔍 Test de connexion à la base de données...
✅ Connexion MySQL réussie
✅ Base de données accessible
🌐 Serveur disponible sur http://localhost:5000
```

### Étape 3: Accéder aux pages

Ouvrez votre navigateur:

**Pour l'administration:**
```
http://localhost:5000/admin_commandes.html
```

**Pour les utilisateurs:**
```
http://localhost:5000/mes_commandes.html
```

**Pour la gestion des utilisateurs:**
```
http://localhost:5000/admin.html
```

## 🎯 Test Rapide de l'API

### 1. Créer une commande de test

Ouvrez un nouveau terminal et exécutez:

```bash
curl -X POST http://localhost:5000/api/commandes ^
  -H "Content-Type: application/json" ^
  -d "{\"user_id\":1,\"montant_total\":25000,\"adresse_livraison\":\"Cocody Angré, Abidjan\",\"telephone\":\"0758415088\",\"notes\":\"Test commande\"}"
```

**Ou** utilisez le script Python de test:
```bash
python test_commandes_api.py
```

### 2. Récupérer toutes les commandes

```bash
curl http://localhost:5000/api/commandes
```

### 3. Vérifier dans l'interface admin

Allez sur: `http://localhost:5000/admin_commandes.html`

Vous devriez voir votre commande de test! 🎉

## 📋 Checklist de Vérification

Cochez au fur et à mesure:

- [ ] Table `commandes` créée dans MySQL
- [ ] Serveur Flask démarre sans erreur
- [ ] Page admin accessible
- [ ] Page utilisateur accessible
- [ ] Création de commande fonctionne
- [ ] Récupération des commandes fonctionne
- [ ] Mise à jour du statut fonctionne
- [ ] Filtrage et recherche fonctionnent

## 🐛 Dépannage Rapide

### Problème: Le serveur ne démarre pas

**Solution:**
```bash
pip install -r requirements.txt
```

### Problème: Erreur de connexion à la base de données

**Vérifiez:**
1. Les credentials dans `server_fixed.py`
2. La connexion internet
3. Que MySQL est accessible

### Problème: La table commandes n'existe pas

**Solution:**
Exécutez le script SQL:
```bash
mysql -u usblj9n0kraq8uoc -p -h bracv1wswmu4vsqxycku-mysql.services.clever-cloud.com bracv1wswmu4vsqxycku < table_commandes.sql
```

### Problème: CORS error

**Solution:**
Le serveur a déjà CORS configuré. Assurez-vous que:
```python
CORS(app, origins="*", methods=["GET", "POST", "PUT", "DELETE", "OPTIONS"])
```
est présent dans `server_fixed.py`

## 📞 Besoin d'aide?

Consultez les fichiers de documentation:
- `README_COMMANDES.md` - Documentation complète
- `SUMMARY_COMMANDES.md` - Résumé de l'implémentation

## 🎨 Fichiers Créés/Modifiés

**Nouveaux fichiers:**
- ✅ `admin_commandes.html` - Interface admin des commandes
- ✅ `mes_commandes.html` - Interface utilisateur
- ✅ `table_commandes.sql` - Script de création de table
- ✅ `test_commandes_api.py` - Tests automatisés
- ✅ `README_COMMANDES.md` - Documentation complète
- ✅ `SUMMARY_COMMANDES.md` - Résumé
- ✅ `QUICK_START.md` - Ce guide

**Fichiers modifiés:**
- ✅ `server_fixed.py` - Ajout des routes API
- ✅ `admin.html` - Ajout du lien vers les commandes

## 🏁 Prêt à démarrer!

Vous êtes maintenant prêt à utiliser le système de gestion des commandes! 🎉

**Bon développement! 🚀**

---

**Prestige Shop Express**
*Votre boutique en ligne de confiance*
